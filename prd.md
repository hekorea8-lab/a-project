# PRD v2 — LangGraph 기반 포스코퓨처엠 회계·세무 검토 시스템

- 상태: 구현 기준 재정의안
- 작성일: 2026-09-07
- 범위: FastAPI 백엔드, LangGraph 검토 오케스트레이션, 승인된 지식기반, 검토의견 기반 PPT
- 원칙: AI는 근거를 찾아 적용 논리를 제시하고, 최종 회계·세무 판단은 담당자가 확정한다.

## 1. 제품 정의

사용자가 자연어로 거래 또는 회계·세무 쟁점을 입력하면 시스템이 질문의 의미와 적용 영역을 먼저 분류하고, 필요한 기준서·법령·해석례·회사 Context를 검색한 뒤, 사실관계·적용 기준·판단 논리·반대 논리·추가 확인사항을 근거와 함께 작성한다. 사용자는 답변을 확인한 후 같은 검토의견을 포스코퓨처엠 보고서 형식의 PPT로 산출한다.

제품의 핵심은 고정 프롬프트가 아니라 LangGraph 상태 흐름이다.

`질문 입력 → 사실·의도 정리 → 적용 영역/기초개념 분류 → 검색 계획 → Hybrid RAG → 근거 재정렬·확장 → AI 검토 → 근거 검증 → 답변·후속질문 → 선택 시 회사 특화 변환/PPT`

## 2. 사용자와 주요 모드

### 2.1 일반 질의 모드

- 법령 정의, 신고기한, 기준서 개념처럼 단순 조회는 짧게 답한다.
- 필요한 경우에만 근거와 적용 범위를 제시한다.
- 단순 질문에는 전문가 검증 호출을 생략해 응답시간을 줄인다.

### 2.2 사실관계 검토 모드

다음 표현이나 입력이 있으면 전문가 검토 모드로 진입한다.

- 검토, 판단, 회계처리, 세무처리, 가능 여부, 리스크, 계약, 특수관계자, 인식, 공제, 가산세
- 금액·기간·거래처·첨부자료가 포함된 거래 질문
- 사용자가 후속 질문을 통해 결론을 좁히는 경우

전문가 모드의 공통 답변 구역은 다음 순서를 유지한다.

1. 사실관계·쟁점
2. 적용 기준
3. 검토 의견
4. 추가 확인

내부 데이터가 부족해도 답변을 중단하지 않는다. 확인된 범위의 잠정 방향을 제시하고 결론을 바꿀 사실과 자료만 요청한다.

### 2.3 포스코퓨처엠 특화 모드

일반 답변의 `포스코퓨처엠 관련 사항으로 검토`를 사용자가 누르면 동일 질문과 기본 답변에 회사 공개자료 Context를 추가해 재검토한다.

- 일반 기준
- 포스코퓨처엠 관점
- 실제 거래 추가 확인

공개 사업보고서·재무제표 주석·IR 자료는 쟁점 후보를 넓히는 보조 근거로만 사용한다. 공개자료만으로 특정 거래의 귀속·회계처리·세무처리를 확정하지 않는다.

## 3. LangGraph 상태 모델

그래프 상태는 하나의 구조화된 객체로 전달한다.

```text
ReviewState
├─ question: 사용자 원문
├─ conversation: 최근 질문·핵심 답변
├─ attachments: 텍스트/PDF/이미지 사실자료
├─ knowledge_track: accounting | tax | composite
├─ review_mode: simple | expert | company_specialized
├─ transaction: 추출된 거래 사실
├─ foundation_analysis: 기초개념·연결 기준서·법령
├─ retrieval_plan: 검색어·후보 주제·누락 사실
├─ evidence_result: 근거 문서·청크·유사도·관계 경로
├─ evidence_pack: LLM에 전달할 정제 근거
├─ answer: 구조화된 AI 답변
├─ validation: 근거·형식·주장 검증 결과
├─ follow_up_questions: 최대 3개 후속 질문
├─ workflow_trace: 단계별 진행상태
└─ errors: 단계별 복구 가능한 오류
```

상태에는 원문 전체를 반복 저장하지 않는다. 로그에는 질문 해시·문서 ID·점수·단계만 저장하고 API 키·첨부 원문은 저장하지 않는다.

## 4. 그래프 노드와 전이

### 4.1 `prepare`

- 질문과 최근 대화를 읽는다.
- 첨부파일은 지시문이 아닌 사실 보강자료로 취급한다.
- 거래처, 금액, 기간, 품목, 계약, 특수관계 여부를 확인된 사실과 미확인 사실로 나눈다.

### 4.2 `classify`

- 회계·세무·복합 트랙을 결정한다.
- 재고자산·원재료, 유형자산, 수익·계약부채, 리스, 개발비, 이전가격·특수관계자, 가산세 등 기초개념을 분류한다.
- 기초개념을 K-IFRS·법령 후보와 연결한다.
- 질문에 명시되지 않은 기준서 번호를 결론 근거로 만들지 않는다.

### 4.3 `plan_retrieval`

- LLM은 결론을 내리지 않고 검색어와 후보 주제만 만든다.
- 모델 호출이 실패하면 검증된 휴리스틱 검색계획으로 전환한다.
- 검색어에는 자연어 품목명과 회계·세무 의미 단위를 함께 넣는다.
  - 예: 리튬 구매 → 원재료·재고자산·매입원가·매입세액
  - 예: 싱가포르 자회사 저가 매입 → 국외특수관계인·정상가격·이전가격·부당행위계산부인
  - 예: 장기공급계약 계약금 → 계약부채·수행의무·통제 이전·K-IFRS 1115 문단 106

### 4.4 `retrieve`

검색 순서:

1. 지식영역 및 문서 유형 필터
2. 문단·조문 정확 검색
3. 키워드/BM25 성격의 검색
4. 벡터 유사도 검색
5. 명시적 법령·기준서 관계 확장
6. 중복 제거·문서 유형 균형·재정렬
7. 동일 기준서 Parent/인접 Child 문맥 확장

검색 결과에는 다음을 유지한다.

- document_id, chunk_id
- 문서명, 문서 유형, source_url
- 조문·문단·섹션
- 시행일·버전·수집일
- 유사도·검색 방법·관계 경로
- citation용 원문 발췌

질의마다 외부 사이트를 실시간 호출하지 않는다. 공식 API·국세법령정보시스템·조세심판원·수동 업로드 기준서는 갱신 작업으로 승인 색인에 저장한다.

### 4.5 `evidence_pack`

LLM에는 검색 결과 전체가 아니라 다음 Pack만 전달한다.

- 확인된 거래 사실
- 적용 후보 기준과 해당 문단·조문
- 직접 근거와 보조 근거 구분
- 인접 문단·Parent 문맥
- 회사 공개자료 Context(특화 모드일 때만)
- 근거별 citation metadata

### 4.6 `generate`

답변 JSON 계약:

```json
{
  "key_answer": "핵심 안내",
  "answer": "[사실관계·쟁점]... [적용 기준]... [검토 의견]... [추가 확인]...",
  "evidence_ids": [],
  "limitations": [],
  "follow_up_questions": [],
  "highlight_terms": []
}
```

전문가 검토 내부 계약에는 확인 사실, 적용 기준, reasoning, counterarguments, requirements, required_evidence, reviewer_actions, improvement_actions, provisional_conclusion, report_draft를 포함한다.

LLM은 검색된 evidence_ids만 인용할 수 있다. 검색되지 않은 조문·문단·예규·판례·회사 사실을 기억으로 보완하지 않는다.

### 4.7 `validate`

- JSON 형식과 필수 구역 확인
- 모든 주장에 허용된 근거 ID만 연결됐는지 확인
- 결론 상태를 `적정 가능성·비적정 가능성·추가 검토 필요`로 제한
- 법령·기준서 문단과 시행일은 metadata에 실제 존재할 때만 표시
- 검증 실패 시 생성 문안을 버리고 근거 확인 필요 답변으로 전환

### 4.8 `follow_up`

현재 답변에서 결론을 실제로 바꿀 수 있는 질문만 최대 3개 제안한다.

- 장기공급계약: 수행의무·검수·통제 이전·계약금 배분
- 이전가격: 비교가능 거래·품질·운송·계약조건·가격산정 정책
- 가산세: 신고 여부·과세표준/세액·법정기한·자진신고일

### 4.9 `company_specialize` 및 `report_pptx`

- 사용자가 명시적으로 선택한 경우에만 회사 Context 재검토를 수행한다.
- PPT는 질문이나 검색결과가 아니라 검증된 검토의견을 입력으로 생성한다.
- 검토의견에 없는 내용을 PPT에 추가하지 않는다.
- 필수 사실이 없으면 추천 입력문구와 함께 사용자에게 요청한다.

## 5. 지식기반과 데이터 모델

### 5.1 회계

- K-IFRS·일반기업회계기준 PDF
- 기준서명·번호·섹션·문단·시행 버전 보존
- Parent는 기준서 섹션, Child는 의미 단위 문단 그룹
- 300~800 tokens 기본, 의미 단위가 긴 경우 최대 1,100 tokens 수준
- 페이지 단위 청킹 금지

### 5.2 세무

- 법령·시행령·시행규칙
- 국세청 예규·해석례·불복 결정례
- 기본통칙·집행기준
- 조세심판원 결정례
- 법제처 판례(현재 세무 답변에서는 별도 정책에 따라 사용 여부를 구분)

### 5.3 회사 Context

- 포스코퓨처엠 사업보고서
- 재무제표 주석
- 공식 IR·공시자료
- 기준기간·공시일·출처 URL·문서 버전 저장

### 5.4 저장소

- 문서·청크 원문과 metadata: PostgreSQL을 운영 목표로 하고 SQLite를 로컬 호환 저장소로 유지
- 임베딩: pgvector
- 관계 그래프: Neo4j
- 컨테이너 재시작 후에도 PostgreSQL/pgvector/Neo4j volume으로 데이터 보존
- Neo4j 장애 시 관계 확장을 생략하고 로컬 검색을 계속한다.

## 6. MCP 도구 계약

모든 도구는 읽기 전용이다.

- `search_law`, `get_law_text`
- `search_precedent`
- `search_nts_taxlaw`, `get_nts_document`
- `search_tax_standard`
- `search_tribunal`, `get_tribunal_decision`
- 기존 `search_knowledge`, `get_document`

MCP 도구는 승인된 로컬 색인을 대상으로 하며, 외부 원천 갱신은 별도 명령으로 수행한다.

## 7. 답변 품질 기준

어려운 질의는 다음을 모두 포함해야 한다.

- 질문의 사실관계 재구성
- 적용 법령·기준서와 적용 이유
- 직접 근거와 보조 근거 구분
- 판단 가능한 부분과 미확인 부분 구분
- 반대 논리 또는 예외 조건
- 필요한 증빙과 담당자 조치
- 실제 검색 근거 기반 citation
- 다음 검토 질문 최대 3개

대표 벤치마크:

1. 특수관계자 싱가포르 자회사로부터 황산니켈 저가 매입
2. Tesla 장기공급계약 계약금 650억원의 계약부채
3. 주민세 사업소분 신고누락 가산세 계산
4. 종합부동산세 부과·징수 일정
5. 유형자산 자산화 요건
6. 리튬 구매의 회계·세무 적용기준 분류

## 8. 검토의견 기반 PPT

표준 목차:

1. 개요
2. 검토사항
3. 검토결과
4. 향후계획

콘텐츠 규칙:

- 표지 상단 날짜·조직
- 핵심 문장은 ○ 불릿
- 보조 설명은 - 불릿
- 수치는 단위 포함
- 비교표는 As-Is/To-Be 또는 기존/개선
- 근거 문서명·조문·문단·시행일·버전 표시
- 계산은 전제·산식·추정 여부 표시
- 미확인 정보는 임의 작성하지 않고 `추가 확인 필요`로 표시
- 부족한 입력에는 담당자가 복사해 사용할 추천 문구를 함께 제시

## 9. 성능·운영 기준

- 단순 조회는 전문가 검증을 생략한다.
- 전문가 호출은 검색계획·사실추출·답변·검증의 단계별 timeout을 분리한다.
- 기본 timeout 합계는 1분 미만으로 유지한다.
- 검색은 로컬 색인을 우선하고 외부 요청을 질의 경로에서 제거한다.
- 단계별 진행상태를 화면의 retrieval trace로 표시한다.
- 실패한 단계는 전체 요청을 중단하기보다 휴리스틱·근거 fallback으로 복구한다.

## 10. 수용 기준

- 동일 질문에서 회계·세무 근거가 섞이지 않는다.
- 검색된 실제 문단·조문만 답변과 PPT에 표시된다.
- 계약부채 질문이 K-IFRS 1115 문단 106으로 연결된다.
- 특수관계자 저가 매입 질문이 법인세·이전가격·관세 쟁점으로 확장된다.
- 계산 질문은 입력값·가정·산식·대안 시나리오를 구분한다.
- 근거가 부족해도 잠정 방향과 필요한 자료를 제시한다.
- 답변에서 PPT 버튼을 누르면 검증된 답변 기반 PPT가 다운로드된다.
- 컨테이너 재시작 후 지식·벡터·그래프 데이터가 보존된다.
- 품질 테스트는 단순 실행 확인이 아니라 복합 질의회신 구조와 citation 연결을 검증한다.

## 11. 제외·보류

- AI가 최종 세무·회계 결론을 확정하는 기능
- 승인되지 않은 외부 사이트의 실시간 검색 결과를 자동 근거로 사용하는 기능
- 사용자 권한을 초과한 내부 원장·민감자료 조회
- 근거 없는 판례·예규·문단 생성
- 사용자가 요청하지 않은 GitHub 커밋·푸시

## 12. 다음 구현 우선순위

1. LangGraph 상태·노드별 trace와 오류 복구 정비
2. 질의 유형별 benchmark fixture 및 실제 질의회신형 평가셋 구축
3. PostgreSQL/pgvector·Neo4j volume 및 운영 마이그레이션 검증
4. 검색어 확장 사전과 기준서·세법 연결 사전 보강
5. 검토의견 기반 PPT 누락정보 요청 흐름 고도화
